spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DATASOURCE_URL:jdbc:postgresql://stats-db:5432/stats}
    username: ${DATASOURCE_USERNAME:stats}
    password: ${DATASOURCE_PASSWORD:stats}
  jpa:
    hibernate:
      ddl-auto: create-drop