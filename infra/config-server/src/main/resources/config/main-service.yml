spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: ${DATASOURCE_URL:jdbc:postgresql://ewm-db:5432/ewm}
    username: ${DATASOURCE_USERNAME:ewm}
    password: ${DATASOURCE_PASSWORD:ewm}
  jpa:
    hibernate:
      ddl-auto: create-drop